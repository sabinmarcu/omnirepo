export type CanvasProperties = {
  color?: string;
};

export type RendererProperties = {
  every: number,
  variance: number,
  size: number,
  speed: number,
  tolerance: number,
};

export type PointType = {
  x: number,
  y: number,
  impulse: number,
};
