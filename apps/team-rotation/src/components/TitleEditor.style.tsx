import { styled } from '@mui/material';

export const TitleEditorWrapper = styled('div')({
  display: 'flex',
  gap: '0.5rem',
  alignItems: 'center',
  justifyContent: 'flex-start',
  paddingBlock: '0.2rem',
  flex: 1,
});
