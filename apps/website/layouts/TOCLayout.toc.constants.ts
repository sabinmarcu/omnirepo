/** Popover target for the drawer-tier TOC. */
export const tocPopoverId = 'toc-drawer';

/** Edge length of the floating drawer trigger; the gutter track adds the rail margins. */
export const tocTriggerInlineSize = 44;
